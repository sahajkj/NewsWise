@model IEnumerable<NewsWise.Models.ReviewPublisher>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Publisher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Site)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Publisher)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Site)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReviewPublisherId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReviewPublisherId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReviewPublisherId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
